## What the Stage Does
1. Launch a web app that takes arguments to load a trained model and run inference on it.

## How to Run
```bash
streamlit run ../utils/app.py --server.maxUploadSize=1000 --server.maxMessageSize=1000
```